#!/bin/bash
set -e

case "$1" in
  purge)
    echo "Removing Siyarix configuration..."
    rm -rf /etc/siyarix
    ;;
  remove|upgrade|deconfigure)
    # Keep configuration on upgrade/remove
    ;;
esac

exit 0
